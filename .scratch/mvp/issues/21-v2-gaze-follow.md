# 21: v2 环视跟随（v1 自动降级）

**What to build:** **休息模式**下 v2 形象视线跟随鼠标指针：指针相对桌宠中心的方位角查 16 向环视表（契约行 9-10，每方向一帧静态姿势）显示对应姿势，指针落在**正前方死区回落 idle**；按形象的 spriteVersionNumber 自动启停——v1 形象（哆啦A梦 / 阿尼亚 / 小悟空）无环视行，功能静默关闭、休息模式保持 idle 不报错；**随机移动期间挂起**（与工单 19 的联动由后落地者接线：本单先落地则预留挂起点，19 后落地则由其接线，反之亦然）；工作模式不跟视（running 常驻）。

背景：spec 故事 62；术语 CodexPetContract（环视行与死区语义来自契约原文："正前方是指针死区，回落到普通 idle"）；[ADR-0010](../../../docs/adr/0010-codex-pet-replaces-oreo.md)。方位判定（指针→角度→16 向）为纯函数，入 Node 确定性回归。

**Blocked by:** 17 模式常驻与生命周期编排

**Status:** done（待手动验收）

- [x] v2 形象：指针绕桌宠一周，16 向姿势按顺时针序正确轮换；正前方死区回 idle；指针静止不动时姿势稳定
- [x] v1 形象：无环视能力静默降级，休息模式保持 idle，无报错无空帧
- [x] 工作模式不跟视；随机移动期间挂起（或挂起点就绪，视与 19 的落地顺序）
- [x] 方位判定纯函数 + Node 回归（角度边界 22.5° 分档、死区判定）
- [x] 手动验收：v2 与 v1 形象各测一轮（设置页切换或临时改默认形象；若工单 22 未落地可临时指定）

实现注记：lookIndex 纯函数（atan2(dx,-dy) 22.5° 顺时针分档、死区 64 逻辑 px ×scaleFactor、负角归一回绕，回归覆盖四正/对角/半角边界/死区/lookCell 行列）；Rust get_cursor_pos（Win32 GetCursorPos 原生 FFI、非 Windows null 静默）；PetWindow syncGazePoll 起停（休息+v2+正常态 30Hz 轮询——窗只有 96×104 指针大多在窗外）、pollGaze 条件复查挂起（拖拽中/sprite.anim 非 idle——随机移动、里程碑、waiting 期间天然排除，跑完恢复，与 19 的挂起点按此接线）。
