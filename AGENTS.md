## 项目内容
- 个人手写阅读文档 "./README.md"，首先了解文件的内容来了解整个项目的面貌。
- 任何情况下都不允许修改阅读文档，只可以给出修改意见。

项目的功能严格按照文档实现，如果功能不清楚那么就询问我而不是自作主张。
前端UI的细节我没有实现，请你结合我的功能进行细节的完善，需要科学、合理。
不要询问任何前端的代码知识，可以询问布局，样式。

## 代码规范
- 代码采取最大复用原则，不要重复造轮子，需要多次使用或者在之后的修改中需要多次使用的功能只用使用同一个方法实现。不断精简代码，在确定代码不会panic的情况下减少条件判断，精简代码。尽可能将代码模块化设计，方便进行功能的单独测试。
  - 为了方便复用，在 "src/FUNC.md" 中编写前端复用的方法信息；在 "src-tauri/FUNC.md" 编写后端复用的方法信息。如果有新的方法需要编写优先从中获取信息看看能否复用。
  - 功能测试需要写好注释：测试的是什么情况，出现什么情况才算正确。
- 代码中类、变量、函数等第一次出现需要写好注释方便review
- 可以自主下载项目需要的skill和extension，以及自主联网搜索。例如为了提高前端效果可以下载高质量的前端skill完成任务。但是需要是项目级的而非全局。

## Agent skills

### Issue tracker

Issue 存放在本仓库 `.scratch/<feature-slug>/` 下的 markdown 文件里（spec + 一文件一 ticket）。见 `docs/agents/issue-tracker.md`。

### Domain docs

Single-context 布局：仓库根一个 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。