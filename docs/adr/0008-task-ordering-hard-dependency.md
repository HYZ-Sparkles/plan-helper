# 任务顺序是硬依赖边

任务间的 A→B 顺序是硬约束：A 完成前，B 不可选入今日分配（大面板置灰展示、不可勾选）。README 中"所有可以并行完成的任务"的口径由此确定：前置依赖已全部完成的任务。

考虑过的备选：软顺序（只影响展示排序，不阻塞推进）。否决理由：子目标层面已经是硬序（必须按序勾选），任务层面用软序不对称；且软序下"顺序"功能退化为排序视图，失去存在意义。

后果：任务数据需要前置依赖字段；大面板计算"可选任务"时要做依赖检查；用户想并行推进有顺序关系的任务，必须先解除顺序。
