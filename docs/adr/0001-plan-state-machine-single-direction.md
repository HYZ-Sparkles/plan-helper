# 计划状态机为单向瀑布

计划状态只允许：未开始 → 进行中 → 已暂停 / 已完成 / 已放弃。**已完成 / 已放弃是终态**，不允许重启——"重新启用"必须以"复制为新计划"实现。已暂停 ↔ 进行中 互转。已暂停 → 未开始 也禁止。

考虑过的备选：把已完成设为可重启，让用户复用一个旧计划。否决理由：下游每个"是不是已结束"的判断都得多一分支，且历史进度会和新进度混在一起。复制一份新计划的成本可忽略（用户已经手动复制），但下游逻辑的简化收益巨大。

后果：UI 上"已完成 / 已放弃"列表里没有"重新开始"按钮，只有"复制并新建"按钮。控制面板的"重新激活"语义不存在。