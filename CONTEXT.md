# 术语表（CONTEXT）

本仓库的领域词汇。每个术语的定义**只**说明它是什么，**不**说明怎么实现。实现决策见 `docs/adr/`。

---

## 计划（Plan）

用户立的一个具体大目标，如"备考英语 6 级"。一个计划由若干**任务**组成。

## 任务（Task）

把计划模块化后的一个**一次性**步骤。一次性：周期性活动（如"每天背单词"）要先汇总成"背完所有 6 级词汇"这种一次性任务。

## 子目标（Sub-goal）

把任务细化为有序步骤的结果。当任务足够简单时可以不分配子目标。

子目标是**二态**的：未完成 / 已完成。

## 子目标的有序性

一个任务的子目标之间有先后顺序，必须按顺序完成。

---

## 计划状态（PlanStatus）

`未开始 | 进行中 | 已暂停 | 已完成 | 已放弃`

—— 状态机规则见 [ADR-0001](docs/adr/0001-plan-state-machine-single-direction.md)。**已完成 / 已放弃是终态**，不能重启。

## 任务状态（TaskStatus）

`未开始 | 进行中 | 已完成`。进度到 100% **自动**转"已完成"（子目标全勾 / 百分比到顶），无需手动确认。已完成的任务锁定：不可再汇报、不可修改字段。

## 计划完成确认（PlanCompletionConfirm）

任务**自动**完成，计划**手动**完成：所有任务都已完成时，计划**停留在"进行中"**，亮起「完成计划」按钮（带确认弹窗），用户点击后才转"已完成"终态。避免"最后一勾、计划瞬间消失"的突兀，也保留"我做到了"的确认感。

## 优先级（Priority）

`Low | Medium | High`

**枚举**，不是数字、不是字符串。Rust 端 `enum Priority { Low, Medium, High }` 配合 `#[derive(PartialOrd, Ord)]`；TypeScript 端用 `as const` 联合类型。

**新计划默认 `Medium`**。理由：低优先级计划可能堆积到不再启动；高优先级会让"我还没想清楚"的新计划占据今日任务配额。

## 任务进度（TaskProgress）

进度**永远是**"耗时完成度"，一条公式两种数据源：

- 有子目标：已完成子目标的预计耗时之和 / 任务预计总耗时
- 无子目标：用户汇报百分比 × 任务预计总耗时

—— 详见 [ADR-0002](docs/adr/0002-task-progress-unified-formula.md)。

进行中修改预计耗时（或增删未完成子目标）时，进度按已完成分钟数**自动缩放**——派生公式的自然结果。

## 进度日志（ProgressLog）

每次进度汇报追加一条事件记录（哪个任务、什么时刻、推进了多少分钟、来源是子目标还是百分比）。任务当前进度、今日完成量、今日总结、[工时账户](#工时账户workhourledger)结算**全部从日志派生**，不固化任何汇总。——详见 [ADR-0009](docs/adr/0009-progress-log-single-source-of-truth.md)。

无子目标任务的汇报是**增量**语义：每次"+X%"（X 为任意正数，最小 0.1%、最多一位小数），任务总进度 = 历次汇报之和。误报修正走任务详情的**修正总进度**（直接设定当前值，带确认，同样以事件落账）；已完成任务的进度不可修正。

## 任务依赖（TaskDependency）

任务间的顺序（A→B）是**硬依赖边**：A 完成前，B 不可选入今日分配（大面板不展示被阻塞任务，2026-08-24 修订——原"置灰展示等待项"砍掉，只展示可选任务）。"所有可以并行完成的任务" = 前置依赖已全部完成的任务。与子目标的硬序（按序勾选）对称。——详见 [ADR-0008](docs/adr/0008-task-ordering-hard-dependency.md)。

依赖边**仅限同一计划内**；删除前置任务时依赖自动解除，后继任务随之解锁。

**录入 UI（DependencyEditor）**：任务卡上的"前置任务"多选选择器（从同计划其他任务中勾选）——对应依赖边模型，所见即所存。控件保持轻量：收在任务卡的**展开态次要区域**，不占常驻空间；**创建表单与计划详情编辑态都提供**（2026-08-23 grill 决策）。

## 推进颗粒度（ProgressGranularity）

推进任务的最小汇报单位：

- **有子目标**：最小单位 = 1 个子目标。子目标不可拆半，要么整推进，要么不推进。
- **无子目标**：单次推进百分比由用户自定——**任意正数（最小 0.1%、最大 100%、最多一位小数）**，默认值 5%（2026-08-24 验收修订：原"只能填 5% 的整数倍"砍掉——庞大而简单的任务需要 0.1% 级细粒度推进）。

**今日推进工作量** 计算使用上述颗粒度：Σ(选中的子目标预计时间) + Σ(无子目标任务 × 汇报百分比 × 预计耗时)。

## 单次推荐百分比（SingleRecommendedPercent）

无子目标任务的单次推进百分比默认值，**默认 5%**。

### 交互形态（PercentAdjustControl）

小看板的百分比调整控件由三部分组成：

- **`[-]` 按钮**（Phosphor `Minus` 图标）：点击 -10%（最小不低于 0.1%）
- **百分比数字（如 `5%`）**：显示当前档位，**用 muted 色**（不突出，提示是次级操作）；**点击数字**弹出数字输入框，可填 0.1%–100% 的任意正数（最多一位小数）
- **`[+]` 按钮**（Phosphor `Plus` 图标）：点击 +10%（最大不超过 100%）

按钮和图标遵循"图标优先"原则（详见 [IconFont](#图标字体iconfont)）。

### 语义

- 每次汇报为**增量**"+X%"，控件设定的是本次汇报量；**不持久化**——切换任务后重置为默认值 5%
- 默认 5% 覆盖常见节奏，直填放开任意正数以适配"庞大但简单"的长任务（如上千次重复动作按 0.1% 推进）
- `SingleRecommendedPercent` 是会话级变量，不进入数据库

## 并行（Parallelism）

- **同优先级内**：多个计划并行推进。控制面板与看板按计划分组呈现，**用户自主**选择今日推进哪些任务。
- **跨优先级**：受[抢占不变式](#抢占不变式preemptioninvariant)约束——详见 [ADR-0006](docs/adr/0006-preemption-invariant-single-tier.md)。

## 抢占不变式（PreemptionInvariant）

**任何时刻，所有"进行中"的计划优先级必然相同**（或没有进行中的计划）。推论：

- **开始约束**：存在进行中的更高等级计划时，低等级计划的「开始」按钮禁用（tooltip 说明原因）。想开低等级计划，先完成 / 手动暂停当前高等级。
- **自动暂停**：更高等级计划开始时，所有进行中的低等级计划自动暂停。**暂停原因（PauseReason）**二值：`自动抢占` / `用户主动`；**用户主动暂停的计划永不自动恢复**。
- **逐层恢复**：当不存在进行中的更高等级计划时（含完成、放弃、**手动暂停**高等级之后），恢复**等级最高的"自动抢占"组**（整组恢复）；更低的被抢占组等这组再清空才轮到。被恢复的计划日后若再次遇到更高等级开始，**再次被自动抢占**（原因仍记"自动抢占"）。
- **自动暂停只针对"进行中"的计划**；未开始的计划不受影响。

### 自动暂停的用户反馈（AutoPauseFeedback）

跨优先级抢占发生时，按强度递减三层反馈：

1. **大看板**（若当前开着）：被暂停的计划立刻标记为灰色、移至"暂停"分组。
2. **控制面板**：今日总结面板插入一行"`计划X` 已被自动暂停（高优先级 `计划Y` 开始）"。
3. **桌宠**：**不**做额外动作（保持"安静"原则）。

不弹模态、不发声、不阻塞用户。

## 今日推进工作量（TodayLoadCommitment）

大面板的硬约束：**选中的任务累计预计耗时 ≥ 当日实际目标**（基准工作时间 + [工时账户](#工时账户workhourledger)结转，透明标注）。

- **推进是硬性的**：累计预计耗时必须 ≥ 工作时间，保证今日够做。
- **完成度是柔性的**：实际推进 ≥ 工作时间就算合规，不要求每个选中的任务都 100% 完成。
- **超额是正常的**：选中的任务累计预计耗时可能远超工作时间（因为任务可部分推进），不需要警告——例如选了 14h 预计耗时的多个任务，今日推进 5h 即可。**选择层面的超额不计入 [工时账户](#工时账户workhourledger) 抵扣**——只有**实际完成**的超额才抵扣。
- **未达标给软提示**：累计预计耗时 < 工作时间时，状态条提示"今日累计 X 小时，差额 Y 小时"。差额按 [工时账户](#工时账户workhourledger) 向后续工作日均分。

口径澄清：硬约束是"**推进**对应工作量"，不是"**完成**对应工作量"——README 初稿的措辞是"必须完成"，grill 中已修订为"必须推进"。

详见 [MainBoardTodayAllocation](#大面板今日任务分配mainboardtodayallocation) 的具体交互。

## 工时账户（WorkHourLedger）

每日实际推进与目标的**差额（无论正负）**都按**均分窗口**（如 7 个工作日，设置中可调）向后续工作日均分：未达标 → 后续每日目标上调；超额 → 后续每日目标下调（**对称抵扣**，总量守恒）。

- **滚动叠加**：当日实际目标 = 基准 + Σ(各历史差额均分到今天的份额)。
- **递归均分**：某个均分目标日当天是工作日却未达标，其新差额继续向后均分——欠的债跟着走。
- **透明显示**：大面板状态条与今日总结显示调整后目标并标注结转（如"目标 5.6h（基准 5.0h + 结转 0.6h）"）。
- **全量实时重算**：任何历史修正（[撤销子目标](#子目标的撤销subgoalundo)、修正总进度）都会重算受影响日期的差额与未来全部目标——不存在"日结冻结"，日志是唯一真相。
- **不设"清账"操作**（grill 决策）：慢性赤字的化解方式只有调大均分窗口；长假等假期应**提前用[日期例外](#工作时间workinghours)排除**，而不是事后抹账。

[DailyCompletionTolerance](#今日达标容差dailycompletiontolerance) 的 ±10% 以**经结转调整后的当日目标**为基数。

**均分窗口以"工作日"为单位**，而非自然日。**今日若不在 [WorkingHours](#工作时间workinghours) 配置的工作日里**，则今日不参与均分计算——今日不计入窗口，也不从历史里扣除。

—— 对称抵扣的取舍详见 [ADR-0007](docs/adr/0007-work-hour-ledger-symmetric-smoothing.md)。

### 今日达标容差（DailyCompletionTolerance）

±10% 容差。例如 5 小时工作计划 → 完成 4.5 小时（4h30m）算达标，4 小时（4h）算未达标。容差带让"差几分钟"不算失败，30% 太松、零容差太僵。10% 和均分窗口配合形成整数比（5h × 10% = 0.5h）。

## 工作时间（WorkingHours）

用户在控制面板中配置两件事：

1. **每日工作时间**（如 5 小时/天）—— 决定 [TodayLoadCommitment](#今日推进工作量todayloadcommitment) 的硬约束数值。
2. **每周工作日**（如 周一/二/三/四/五）—— 决定 [工时账户](#工时账户workhourledger) 的均分窗口与今日是否需要"今日任务分配"。

**日期例外（DateOverride）**：周循环之上允许按日期**双向覆盖**——"这天不工作"（假期，把周模式的工作日覆盖为休息）或"这天工作"（调休补班，把周模式的休息日覆盖为工作）。提前标注、将来生效；"今日是否工作"的判定 = 周循环 + 日期例外合并的结果。

每日内可**配置若干段时间窗口**（如 `10:00–11:30, 20:00–22:00`），而不是单段连续时间。一天可能跨多个工作段。

**今日是否工作**：今日若不在工作日里，大面板**不自动弹**（自动触发 = 启动序列后检测 / 工作窗口开始）；但**手动切入工作模式 = 主动加班**——大面板照常打开并可分配（2026-08-29 用户决策，取代原"分配不加载"的休息日态 UI）。非工作日的推进照常记入任务进度，工作量上按**超额**并入 [工时账户](#工时账户workhourledger)。

### 设置改动的生效时机（SettingsEffectiveTime）

**每日工作时间 / 每周工作日 / 时间窗口**的改动**自下一个工作日生效**——当日维持原值，今日目标、达标判定、总结均不受影响。**均分窗口**的改动**立即生效**（它只影响未来的分摊计算）。

### 今日总结的触发（DailySummaryTrigger）

**最晚**的一个工作时间窗口结束那一刻，自动弹出今日总结。如果当天用户根本没打开应用（所有工作段都过了），则**第二天第一次打开应用**时弹出，标题改为"**昨日总结**"——视为对前一天的补登。

不依赖用户主动点"今日工作结束"按钮。

**只弹一次**：触发后新产生的进展**不重弹**——总结内容从 [ProgressLog](#进度日志progresslog) 实时重算，用户可在控制面板中随时调出查看最新版本（次日补登的"昨日总结"同理）。

**跨午夜窗口**：工作窗口跨过 0 点时（如 20:00–01:00），窗口内的汇报与总结归属**窗口开始时刻的日期**所在的工作日。

## 首次启动（FirstRun）

无 onboarding 向导：启动序列播完直接上桌面。默认**工作时间 5h/天、工作日周一至五、均分窗口 7 个工作日**（三者同时是全局兜底值，设置页随时可改）。控制面板"计划管理"页用空状态 CTA 引导"创建第一个计划"。不提供开机自启开关（想要的用户可自行放入 `shell:startup`）。

## 今日总结的布局（DailySummaryLayout）

今日总结的视觉布局（自动触发时机见 [DailySummaryTrigger](#今日总结的触发dailysummarytrigger)）：

### 顶部总览行

一行显示核心数字：

- **今日推进 N 计划**（N = 今日有推进的计划数）
- **完成 X 小时**（实际推进的工作量）
- **目标 Y 小时**（当日实际目标，含结转标注——见 [工时账户](#工时账户workhourledger)）
- **进度条**：X / Y 比例可视化（参考 [DesignTokens](#设计tokendesigntokens) 强调条）
- 图标：Phosphor `CheckCircle`（完成）+ `Target`（目标）

### 下方分块列表

按"**计划 → 任务 → 推进内容**"三层嵌套：

- **计划 section**：每个推进过的计划一个 section header（计划名 + 优先级 [Phosphor 图标](#优先级几何符号priorityglyph) + 今日推进总耗时）
- **任务行**：每个推进的任务一行（任务名 + 推进百分比 + 推进耗时）
- **推进内容**：
  - 有子目标：显示完成列表（如 `✓ 子目标2 / 子目标3 进行中`）
  - 无子目标：显示百分比进度条（线条风格，参考 [DesignTokens](#设计tokendesigntokens)）

展示**所有当日有推进的计划**——包括中途被[抢占](#抢占不变式preemptioninvariant)暂停的：干了的活就是干了，该计划的 section 标注"已被抢占暂停"，其推进量**计入完成总量与达标判定**。当日零推进的计划不展示。

## 删除任务（TaskDeletion）

删除按钮**永远可点**，但删除任务时弹"将丢失 X 小时进度，确认？"二选一对话框，且必须打字 `再删` 才能点确认。已删除任务的进度进归档（不是真删）。

## 子目标分配（SubGoalAllocation）

任务是否有子目标**完全由用户决定**——系统不设耗时阈值强制要求子目标。子目标是简化汇报（把"完成百分比"换成"勾选清单"）的工具，不是任务复杂度的判定。

### 子目标的撤销（SubGoalUndo）

已完成子目标的勾选框**可点**（取消完成回到未完成），但撤销时弹一行 toast：「已撤销 X 的完成，进度已重算」——让用户明确知道进度被改了。"已完成不允许修改"指的是**任务 / 子目标的字段（名称、耗时）**，勾选状态本身可以撤销。

---

## 桌宠（Pet）

像素风格动物（Oreo Cat）的桌面角色。挂在桌面上、有固定动画集、随模式与事件触发动作。

只保留 Oreo Cat 一种资源——grill 决策中 Natori 因资源与风格取舍被移除。若未来需要"二次元模式"再单独评估，详见 [ADR-0005](docs/adr/0005-visual-tone-line-warm.md) 的"桌宠视觉语言保持独立"原则。

## 工作模式 / 休息模式（WorkMode / RestMode）

桌宠有且仅有两个模式：

- **工作模式**：桌宠常驻 Sleep Idle（循环），**安静陪伴**。
- **休息模式**：桌宠在 Stand Idle（循环）/ Sit Idle 之间切换，并按概率触发随机动作。**小看板隐藏**（休息即不工作），切回工作模式恢复显示。

应用启动（启动序列完成后）的**初始模式按工作时间判定**（2026-08-29 用户决策，取代原"默认工作模式"）：现在是工作时间（工作日 + 当前时刻在某段时间窗口内）→ 工作模式；否则 → 休息模式。AutoOpenMainBoard 的"启动序列完成后检测"触发以工作模式为前提。模式是会话级状态，不持久化，重启重新判定。

用户点击桌宠在两个模式间切换，或从任一模式进入**控制面板**。

## 桌宠动作执行（PetActionExecution）

桌宠动画的执行模型：**当前动作锁**（不再使用 FIFO 队列）——grill 中确立的最简模型。

### 状态

桌宠只有两个状态：

- **空闲（Idle）**：无动作在播，可以接受新动作
- **播放中（Playing）**：有动作在播，新动作被拒绝

### 用户主动动作的独占性

用户主动触发的动作（点击菜单项、拖动后 Attack、切换模式）**独占当前动作锁**：

- 触发后立即进入"播放中"，锁住后续用户动作触发
- 动作播放完毕才回到"空闲"
- 播放期间，用户**不能再次触发**桌宠动作——按钮禁用、点击无效
- 播放期间，**拖拽仍然可用**（位置变更与动画独立）
- 播放期间，**菜单可打开查看**，但菜单项**点击触发无效**

### 系统自动动作无锁

系统自动触发的动作（休息模式随机 Eat / Jump）**不占用锁**：

- 仅在"空闲"状态触发
- 触发后立即执行（无需排队）
- 执行完毕回到"空闲"
- 可连续出现（如连续 Eat / Jump，无去重顾虑——既无去重也无容量上限）

### 流畅性保障

这是模型的关键质量约束，所有交互细节围绕"流畅"：

- **按钮禁用的视觉反馈**：播放中的按钮显示 muted 色 + 鼠标光标变 `not-allowed` + hover tooltip "桌宠正在执行动作"——让用户明确知道"现在不能点"而非"点了没反应"
- **拖动不打断动画**：拖动时桌宠位置变更，但当前动画帧继续播（参考 README 的"鼠标拖动：保持当前帧不动，移动完了才继续"）
- **动作衔接丝滑**：动作 A 末帧 → 动作 B 首帧自然过渡（避免硬切）；具体由 Oreo Cat 帧序列决定，同一作者帧通常可衔接；无法衔接时退化为"快速淡出 → 淡入"避免跳变
- **菜单查看无副作用**：打开菜单查看时不影响当前动画播放，关闭菜单后动画继续

### 启动序列期间

启动序列是"应用未进入正常状态"的过渡期，**所有交互禁用**（包括拖拽和菜单）——比"动画播放中"更严格。详见 [StartupSequence](#启动序列startupsequence)。

—— 实现细节，未作为 ADR 记录。

## 桌宠随机动作（PetRandomAction）

休息模式下的非用户触发动作（跑去吃饭、来回跳）。当前权重硬编码：**吃 : 跳 : 闲坐 = 4 : 3 : 3**。概率值与触发间隔字段在 `PetConfig` 中预留，**未做配置 UI**。**触发间隔：休息模式下，从上一次"用户动作或随机动作"结束算起，固定 300 秒（5 分钟）**。工作模式不抽随机动作（与"安静"原则一致）。

—— trade-off 弱，未作为 ADR 记录。

## 桌宠与看板的耦合（PetBoardCoupling）

小看板和桌宠**可以一起拖动**，但桌宠的动作不会带着小看板一起动。两者位置联动通过共享坐标实现，**动作独立**。

---

## 视觉基调（VisualTone）

桌面应用整体走**线条风格 + 暖色调基调**：1px 边框 + 极浅色块填充 + 几何元素（圆点/线段/矩形徽章）作为主要视觉语言。

主色为暖橙（`#FB923C`），状态色三档（暖绿/暖黄/红）—— 整体往暖色方向倾斜。**禁用 emoji 符号**（🔴/🔵/⚪ 等），改用文字标签 + 几何符号或图标字体。

桌宠视觉语言**独立保留**（像素 Oreo Cat，不被 UI 风格同化）。

—— 详见 [ADR-0005](docs/adr/0005-visual-tone-line-warm.md)。[ADR-0004](docs/adr/0004-visual-tone-warm-light.md)（暖白 + 阴影系统）已被 superseded。

### 设计 token（DesignTokens）

| Token | 值 |
|---|---|
| 背景（base） | `#FFFFFF` |
| 背景（分组） | `#FAFAF9`（stone-50）/ `#FFF7ED`（orange-50 强调分组） |
| 表面（surface） | `#FFFFFF` |
| 边框（default） | `1px solid #F5F5F4`（stone-100） |
| 边框（active） | `1px solid #FB923C`（orange-400） |
| 强调条（active bar） | `3px solid #FB923C`（顶部选中条） |
| 主色（primary） | `#FB923C`（orange-400）；hover `#F97316`（orange-500） |
| 状态色（完成） | `#16A34A`（green-600） |
| 状态色（进行） | `#D97706`（amber-600） |
| 状态色（警示） | `#DC2626`（red-600） |
| 文字 primary | `#1C1917`（stone-800） |
| 文字 secondary | `#57534E`（stone-600） |
| 文字 muted | `#A8A29E`（stone-400） |
| 阴影 lg（仅浮层） | `0 10px 25px rgba(0,0,0,0.1), 0 4px 6px rgba(0,0,0,0.05)` |
| 圆角 | 8px / 12px / 16px（无 4px、无 24px） |
| 字体栈 | `Inter, "SF Pro", -apple-system, sans-serif`（中文 fallback：`"PingFang SC", "Microsoft YaHei"`） |

### 图标字体（IconFont）

| Token | 值 |
|---|---|
| 图标库 | **Phosphor Icons**（https://phosphoricons.com） |
| 默认风格 | `regular`（线条圆润，1.5px 描边） |
| 备选风格 | `thin`（轻量场景）/ `bold`（重点强调） |
| 视觉重量 | 16-24px，与正文行高匹配 |

**图标优先原则**：按钮、菜单、状态、优先级等通用交互元素能用图标传达的**优先用图标**——鼠标 hover 显示 tooltip 解释含义。这样能：

- 增加 UI 可利用空间（小看板 / 工具栏尤其受益）
- 与"线条 + 几何"调性契合
- 替代 emoji，视觉语言统一

例外保留文字标签：

- 主操作（如"创建计划"、"确认"、"再删"等高破坏性操作必须有文字）
- 优先级文字（"高 / 中 / 低"）
- 必填字段说明
- 状态语义不明确时

### 优先级视觉体现（PriorityVisuals）

**禁用 emoji**（如 🔴/🔵/⚪），改用"**文字标签 + Phosphor 图标**"的组合，详见 [PriorityGlyph](#优先级几何符号priorityglyph)。

- **列表项**：左侧 4px 边条颜色 + 标题旁的"标签 + 图标"。
- **单独显示**：仅标签 + 图标，例如 `高 [CaretUp] / 中 [Minus] / 低 [CaretDown]`。
- **白底加深饱和度**：High 红加深、Low 灰提亮，避免被白色稀释。

| 优先级 | 边条色 | 标签底色 | 标签文字 | 图标 |
|---|---|---|---|---|
| High | `#DC2626`（red-600） | `#FEE2E2`（red-100） | "高" / "High" | Phosphor `CaretUp` |
| Medium | `#3B82F6`（blue-500） | `#DBEAFE`（blue-100） | "中" / "Medium" | Phosphor `Minus` |
| Low | `#94A3B8`（slate-400） | `#F1F5F9`（slate-100） | "低" / "Low" | Phosphor `CaretDown` |

### 优先级几何符号（PriorityGlyph）

**Phosphor Icons** 提供，default 风格 `regular`：

- High → `CaretUp`（向上三角，呼应"优先级高"语义）
- Medium → `Minus`（水平线，呼应"中性"语义）
- Low → `CaretDown`（向下三角，呼应"优先级低"语义）

选用 Caret 系列是因为它**既几何又暗示方向**——比字母徽章更"特色"（呼应 grill 中"颜色和文字更特色"的要求），比纯几何符号（▲▼●○）更精致且与 Phosphor 库风格统一。

## 实施粒度（ImplementationGranularity）

主流程的工单按 **tracer-bullet vertical slice** 切：每张工单自包含、能装进一个全新上下文窗口、完成后可丢弃上下文。**不在工单内拆 sub-task**。

---

## 桌宠交互（PetInteraction）

- **单击桌宠**：弹出菜单（含「控制面板 / 切换模式 / 再见」三项）。
- **拖拽**：可拖动到屏幕任意位置（边界约束见 PetDragBounds）。
- 鼠标点击区域：**只**响应桌宠本体，不响应小看板。

### 菜单项语义（PetMenuActions）

- **控制面板**：打开独立窗口的控制面板（与模式无关）。
- **切换模式**：工作 ↔ 休息切换。
- **再见**：**退出整个应用**——桌宠播放「跳入箱子」动画（Oreo Cat 帧 17），动画完成后关闭所有窗口（桌宠、大看板、小看板、控制面板）。下次启动重演「启动应用」序列（21 Ear Up → 22 Scan → 23 Ear Down → 24 Jump out the box → 7 Stand Idle）。

## 系统托盘（SystemTray）

应用常驻系统托盘：

- **左键单击**：打开控制面板
- **右键菜单**：「打开控制面板」「退出」——退出入口与桌宠菜单「再见」并存，两者都走跳箱告别动画（Oreo Cat 帧 17）后关闭全部窗口

**窗口关闭语义**：桌宠窗口无边框、**无关闭按钮**，系统关闭请求（Alt+F4）被拦截——退出只能走托盘或「再见」。控制面板 / 大面板 / 小看板的关闭 = **隐藏窗口**，不退出应用。

## 启动序列（StartupSequence）

应用启动时桌宠播放的动画序列：**21 Ear Up → 22 Scan → 23 Ear Down → 24 Jump out the box → 7 Stand Idle**。

启动序列期间**不响应任何用户交互**（点击、菜单、拖动都忽略）。序列完成后才进入正常应用状态。

[AutoOpenMainBoard](#自动打开大面板autoopenmainboard) 的检测在启动序列完成后才进行。

## 自动打开大面板（AutoOpenMainBoard）

**原则**：任何时候应用处于工作模式且今日未分配任务，自动打开大面板让用户选择。

### 触发时机

- 自行设置的[工作时间](#工作时间workinghours)窗口**开始时**
- 用户手动从休息模式切换到工作模式时

### 启动应用的特殊性

启动应用时先播放[启动序列](#启动序列startupsequence)——**序列完成后再检测条件**。如果检测到"工作模式 + 未分配"，自动弹出大面板。

启动序列不会被大面板打断——大面板在桌宠站立后才弹出。

### 不触发的场景

- 今日已分配任务（之前会话里已选过今日推进列表，未触发新工作日开始）
- 处于休息模式
- 今日不在工作日里（参见 [WorkingHours](#工作时间workinghours)）

## 拖动边界（PetDragBounds）

四个约束：

1. **任务栏区域不可拖入**——避免桌宠被任务栏遮挡、无法拖回。
2. **不能拖到屏幕物理边界外**——桌宠始终全可见，用户随时能找到。
3. **多显示器之间可拖**——按主显示器为基准的相对坐标，跨屏时按方向正确切换。
4. **边缘吸附**——拖动停止时若桌宠靠近屏幕边缘（< 20px）自动吸附到边缘。便于整理桌面、腾出中央空间。

## 创建流程的 UI 模式（CreationUI）

**单页可折叠表单**，不是向导。三个段独立（自上而下）：

- **Plan 段**（默认展开）：名称、简述、详细内容（[自适应 textarea](#详细内容字段)）、优先级、截止日期。
- **Task 列表段**（默认折叠）：可增删的任务卡。任务卡**默认收起为一行摘要**（序号 + 名称 + 预计耗时/子目标标记），点击展开完整字段（名称、简述、详细内容、是否需要子目标、前置任务）——长字段只在展开态出现，列表紧凑（2026-08-23 grill 决策）。
- **SubGoal 列表段**（默认折叠）：每个任务的子目标列表，含内容、预计耗时、顺序。

### 详细内容字段

Plan 段和 Task 段的"详细内容"字段形态：

- **类型**：textarea（**纯文本**，不支持 Markdown——后期 LLM 拓展再考虑）
- **字数**：**无上限**——用户可写任意长度
- **高度**：自适应——最少 3 行高，长文本按内容自动拓展；达到 8 行高后开始滚动
- **用途**：给用户自己记录详细信息（计划背景、任务的注意事项、参考资料等），不渲染为 Markdown

简单可靠的"长文本输入"——避免 LLM 渲染复杂度，同时支持任意长度的描述。

### 子目标填写表单（精简输入行）

子目标与任务的 UI 形态**完全不同**——子目标是"小且原子化"的步骤，不需要任务那种"较大且可能有联系"的卡片结构。

#### 出现条件

每个任务卡有一个**"是否填写子目标"切换按钮**（Phosphor `ListChecks` 图标 + 文字标签）：

- **默认未勾选**：该任务的 SubGoal 列表**不显示**，任务只能汇报百分比（按 [ProgressGranularity](#推进颗粒度progressgranularity) 无子目标规则）
- **勾选后**：该任务的 SubGoal 列表**展开**，可按下方精简表单连续填写
- **取消勾选**：删除已填子目标（弹确认 toast："将删除 X 个子目标，确认？"）

按钮遵循 [IconFont](#图标字体iconfont) 图标优先原则——hover tooltip "需要子目标吗？"

#### 精简输入行

每个子目标一行紧凑输入：内容 + 预计耗时（内容必填单行、耗时必填数字默认小时）。
- 行末 **`+`** 按钮（Phosphor `Plus`）：点击添加下一行
- 在任意输入框按 **回车**：自动添加下一行并聚焦到内容框

子目标列表就是从第一个子目标开始往下填的"**连续输入行序列**"——这是**流式输入**模式，与任务的卡片管理形成对比。

#### 顺序与修改

- **任务顺序 = 创建顺序，不可拖拽、不加序号**（2026-08-24 用户决策砍掉原"任务卡拖拽排序"，同日复审砍掉序号标识）：先后语义唯一归[依赖边](#任务依赖taskdependency)，同 [PlanOrdering](#计划排序planordering) 砍手动排序的原则——实施顺序在大面板自主选择、由依赖制约。不加序号的理由：任务的真实顺序是**偏序**（只有依赖边连接的任务间有先后），编号表达全序，对偏序集合必然误导（两条独立链、创建序与链序不一致时数字乱序/重复）。顺序关系由依赖边展示承载：**详情页"前置：A（谁挡我）/ 被等待：B（我挡谁）"双向行**、编辑态 DependencyEditor、大面板只展示可选任务。已完成任务自动沉底、追加任务落在未完成之后（服务端归一化不变）；子目标行以圆圈符号承载行标识，同样不加序号
- **子目标顺序 = 创建顺序**（填写顺序），**不允许拖拽改顺序**（参见 [PlanOrdering](#计划排序planordering) 的"默认排序"原则——子目标不在拖拽范围内）
- 单条子目标可删除（弹确认），后续子目标顺序自动递进
- 已完成子目标的"撤销完成"按 [SubGoalUndo](#子目标的撤销subgoalundo) 规则

用户能**看到全貌**、**随时回头修改已填好的段**。验证逻辑按段独立，不采用向导的"必须上一步下一步"。

### 校验与追加

- **计划保存至少 1 个任务**（不允许空计划）；勾选"需要子目标"的任务**至少 1 个子目标**。
- **进行中的计划允许追加新任务**——新任务排在所有未完成任务之后，依赖与顺序照常编辑（补 README"增删改"规则未覆盖的新增场景）。
- 该表单组件是**创建/编辑双模式**：计划详情页的编辑态复用同一表单（2026-08-23 grill 决策），字段与校验完全一致。

---

## 大看板 / 小看板（MainBoard / MiniBoard）

大看板：每天第一次进入工作模式时的今日任务选择界面，固定在"控制面板 → 打开大面板"打开。小看板：常驻桌宠附近，显示当前任务的子目标。

桌宠与小看板窗口**置顶**（always-on-top）；大面板与控制面板为**普通窗口**（要与资料、编辑器并排使用）。小看板底部常驻**今日总量微型进度条**（今日推进 X / 调整后目标 Y），呼应 [工时账户](#工时账户workhourledger) 的透明显示原则。

### 当前任务（CurrentTask）

小看板显示的、用户**此刻正在做**的任务。用户从大看板选完今日推进列表后，手动指定一个任务为"我先做这个"。控制面板和小看板都跟它走。

当前任务完成（自动转已完成）后，小看板**空态停留**："✓ 任务完成" + 「更换任务」按钮，等用户主动选下一个，**不自动切换**。当前任务所属计划被暂停 / 放弃、或任务被移出今日推进列表时，同样回到空态。

### 更换任务（SwitchCurrentTask）

小看板右上角"更换任务"按钮：点开后从今日推进列表里挑另一个。被选过的优先从**同一计划**开始（同计划的备选先列前面）。这避免"我正在做计划A，突然切到计划B"的突兀感。

## 大面板今日任务分配（MainBoardTodayAllocation）

大面板只展示**当前可推进**的计划任务——用户在最高优先级的进行中计划里选今日推进哪些任务。

### 过滤与分组

- **过滤**：取所有 `PlanStatus = 进行中` 的计划，按 `Priority` 降序排序——[抢占不变式](#抢占不变式preemptioninvariant)保证它们必然同等级，**无需"只显示最高等级"特判**
- **依赖阻塞**：前置任务未完成的任务**不进入列表**——只展示可选任务（2026-08-24 修订，原"置灰展示等待项"砍掉；详见 [TaskDependency](#任务依赖taskdependency)）
- **分组**：按计划分组展示（每个计划一个 section，section header = 计划名 + Phosphor 图标）

### 交互

- **单页连续**：所有任务列表展示，用户勾选的同时实时累计预计耗时
- **底部状态条**：固定显示"今日累计 X.X 小时 / 当日实际目标 Y 小时（基准 + 结转）"
- **软提示**：
  - 累计预计耗时 < 工作时间：黄色提示"差额 Y 小时"（参见 [TodayLoadCommitment](#今日推进工作量todayloadcommitment)）
  - 累计预计耗时 ≥ 工作时间：**无提示**（这是正常状态）
- **不显示超额警告**：累计预计耗时 > 工作时间是预期行为（任务可部分推进），不需要警告——例：选 14h 预计耗时的多个任务，今日推进 5h 即可
- **确认按钮**：底部中间，点击后大面板关闭

### 与硬约束的关系

硬约束是 [TodayLoadCommitment](#今日推进工作量todayloadcommitment) 中定义的"累计预计耗时 ≥ 工作时间"——选完后状态条按达标/未达标给软提示，但**不阻止提交**（柔性边界）。

## 控制面板导航（ControlPanelNav）

控制面板是独立窗口，**侧边栏导航**（固定 3 项，不预期大幅膨胀）：

| 导航项 | 内容 | 图标 |
|---|---|---|
| 计划管理 | 计划列表（状态筛选栏）+ [计划详情](#计划详情plandetail)入口 + 打开大面板按钮 + 今日总结 | Phosphor `ListBullets` |
| 创建 | 新建计划（详见 [CreationUI](#创建流程的ui模式creationui)；编辑复用同一表单，位于详情页） | Phosphor `Plus` |
| 设置 | 工作时间 / 工作日 / 均分窗口 / 桌宠偏好 | Phosphor `Gear` |

**今日总结的位置**：放在"计划管理"页**右侧的小栏目**，或者作为按钮点开查看。不单独占一级导航。

**未来拓展**：LLM 相关功能作为"计划管理"内的二级功能（生成任务按钮等），不新增一级导航项。

## 计划排序（PlanOrdering）

计划列表的展示顺序规则（控制面板"计划管理"页 + 大面板今日任务分配）：

### 默认排序（唯一排序）

按 **`Priority` 降序 + `CreatedAt` 倒序** 自动排：

- 优先级最高的在前（High → Medium → Low）
- 同优先级内最新创建的在前
- 不需要用户干预——创建即展示

**无手动覆盖**（2026-08-24 用户决策砍掉原"手动拖拽排序"）：计划的实施顺序在[大面板](#大面板今日任务分配mainboardtodayallocation)中自主选择、由优先级制约，计划列表没有"必须的先后次序"，手动排序无意义——默认排序只承担"优先级与新近度自动组织"。

## 计划详情（PlanDetail）

计划内容的独立页面（从计划管理列表点击计划卡进入，返回则回列表）：

- **头部**：计划名 + 优先级标签 + 状态徽章 + 生命周期操作（开始 / 暂停 / 放弃 / 复制并新建，按当前状态可得）+ 「编辑」按钮。
- **正文**：计划字段（简述 / 详细内容 / 截止日期）与任务列表（任务 → 子目标层级，含进度）。
- **编辑态**：点「编辑」后正文切换为 [CreationUI](#创建流程的ui模式creationui) 表单（同一组件的创建/编辑双模式）；生命周期按钮不进编辑态。

计划数量少而任务/子目标数量多，调整任务与子目标需要整页空间——因此详情是独立页面，不挤侧栏或弹窗（2026-08-23 grill 决策）。

---

## 计划管理视图（PlanManagementView）

"计划管理"页的组织方式（2026-08-23 grill 修订，取代原 4-tab 方案——tab 与状态模型对不齐：状态 5 个 tab 只有 4 个）：

- **状态筛选栏**：一行筛选项「全部 / 未开始 / 进行中 / 已暂停 / 已完成 / 已放弃」，每项带数量；五个状态都是一等公民。默认「全部」。
- **列表**：按 [PlanOrdering](#计划排序planordering) 排序的计划卡；点击卡片进入该计划的[详情页](#计划详情plandetail)。
- 终态计划（已完成/已放弃）不可重启、无"重新开始"，只有"复制并新建"；**计划不能彻底删除**——[放弃计划](#放弃计划abortplan)是唯一的"离开进行中"的方式。

### 复制并新建（CopyAsNewPlan）

终态计划（已完成/已放弃）的"重启"等价操作——**一键复制**旧计划为全新计划。

**触发**：计划详情页头部（终态计划显示此按钮）。

**一键复制规则**（无弹窗选择）：

- **复制**：计划名称、详细内容、优先级、截止日期、任务列表（含子目标列表）
- **修改**：新计划名称自动加 `- 副本` 后缀（如"备考英语 6 级 - 副本"），用户可在编辑模式改
- **归零**：所有任务的进度、所有子目标的完成状态
- **状态**：新计划**直接进入"进行中"**——一键复制 = "复制模板 + 立即开始"

**结果**：

- 旧计划保持终态不变（仍在"已完成 / 已放弃"筛选下可见）
- 新计划出现在"进行中"筛选下
- 用户可在编辑模式调整任意字段（名称、优先级、任务内容等）

## 放弃计划（AbortPlan）

"放弃"是 [PlanStatus](#计划状态planstatus) 的状态转换之一：`进行中 | 已暂停 → 已放弃`（终态）。

### 触发流程

沿用 [TaskDeletion](#删除任务taskdeletion) 的二次确认 UI 流程：

1. **一级确认**：弹窗显示"将放弃 `计划名`，将保留 X 个任务 / Y 小时进度作为历史"
2. **二级确认**：用户必须打字 **`再删`**（沿用任务删除的措辞）才能点确认按钮——防误操作
3. **确认后**：PlanStatus = 已放弃，进度进归档（不是真删，可随时查看）

### 与删除的区别

- **删除任务**：任务从数据库"软删除"（进归档，不真删）—— 未来可能"恢复"
- **放弃计划**：PlanStatus 进入终态"已放弃"——未来只能"复制并新建"重启

"放弃"在语义上是"承认计划失败"，与"软删除任务"（撤销错误操作）的语义不同。**沿用删除任务的确认流程只是 UI 一致性，底层操作不同**。

---

## 名词单复数（避免被过度加载）

| 概念 | 单数 | 复数 |
|---|---|---|
| 计划 | Plan | Plans |
| 任务 | Task | Tasks |
| 子目标 | SubGoal | SubGoals |
| 大看板 | MainBoard | — |
| 小看板 | MiniBoard | — |